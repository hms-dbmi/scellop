---
"@scellop/data-loading": patch
---

Toolchain-only release. The package is built with Vite 8 and TypeScript 7 now, so the emitted bundle and declarations differ from the previous release, but there is no source or API change.
